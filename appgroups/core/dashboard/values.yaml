kubernetes-dashboard:
  app:
    ingress:
      enabled: true
      useDefaultIngressClass: true
      issuer:
        name: disabled
      paths:
        - /
      tls:
        - secretName: wildcard-tls
    