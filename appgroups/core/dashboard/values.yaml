kubernetes-dashboard:
  app:
    ingress:
      enabled: true
      useDefaultIngressClass: true
      issuer:
        name: disabled
      paths:
        - /
      annotations:
        traefik.ingress.kubernetes.io/service.serversscheme: https  # ADD THIS