kubernetes-dashboard:
  app:
    enabled: true
    ingress:
      enabled: true
      ingressClassName: traefik
      path: /dashboard
      issuer:
        name: cluster-issuer
      tls:
       secretName: wildcard-tls