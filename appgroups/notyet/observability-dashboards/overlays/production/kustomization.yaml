apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

bases:
  - ../../base

# Add any production-specific patches here
# For example, adjusting refresh rates or thresholds

commonLabels:
  environment: production
