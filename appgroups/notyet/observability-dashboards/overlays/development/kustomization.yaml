apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

bases:
  - ../../base

# Development-specific overrides
# Could adjust refresh rates for faster iteration

commonLabels:
  environment: development
